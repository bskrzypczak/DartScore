<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Bazowy motyw okna. Właściwy UI rysuje Compose (DartScoreTheme).
         Parent ustawiony na motyw frameworka, by nie ciągnąć dodatkowych zależności XML. -->
    <style name="Theme.DartScore" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
